@page
@model PersonalDataModel
@{
    ViewData["Title"] = "GDPR";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="row">
    <div class="col-md-6">
        <p>Ditt konto innehåller personlig data som du har givit oss. Denna sida tillåter dig att ladda ner eller radera den datan.</p>
        <p>
            <strong>Genom att radera din data kommer den försvinna för gott och kan ej återställas.</strong>
        </p>
        <form asp-page="DownloadPersonalData" method="post" class="form-group">
            <button class="btn btn-default" type="submit">Ladda ner</button>
        </form>
        <p>
            <a asp-page="DeletePersonalData" class="btn btn-default">Radera</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}